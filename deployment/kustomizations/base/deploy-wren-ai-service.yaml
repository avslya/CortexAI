apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-ai-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cortex-ai-service
  template:
    metadata:
      labels:
        app: cortex-ai-service
    spec:
      containers:
        - name: cortex-ai-service
          image: ghcr.io/canner/cortex-ai-service:latest
          volumeMounts:
            - name: config-volume
              mountPath: /app/data
          env:
            - name: cortex_AI_SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: cortex-config
                  key: cortex_AI_SERVICE_PORT
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: cortexai-secrets
                  key: OPENAI_API_KEY
            - name: QDRANT_HOST
              valueFrom:
                configMapKeyRef:
                  name: cortex-config
                  key: QDRANT_HOST
            - name: LOGGING_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: cortex-config
                  key: LOGGING_LEVEL
            - name: cortex_UI_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: cortex-config
                  key: cortex_UI_ENDPOINT
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: LANGFUSE_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: cortexai-secrets
                  key: LANGFUSE_PUBLIC_KEY
            - name: LANGFUSE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: cortexai-secrets
                  key: LANGFUSE_SECRET_KEY
            - name: CONFIG_PATH
              value: /app/data/config.yaml
          ports:
            - containerPort: 5555
      volumes:
        - name: config-volume
          configMap:
            name: cortex-ai-service-config
            items:
              - key: config.yaml
                path: config.yaml
